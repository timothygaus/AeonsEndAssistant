server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router owns the routing, so unknown paths fall back to the app
    # shell rather than 404ing.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
